const EventHub = require('./index')

describe('EventHub', () => {
  it('calls the listener when the event happens', () => {
    let called = false

    // TODO: test the event hub here and fix the assertion

    expect(called).toEqual(true)
  })

  // TODO: write any more tests that you think this module should have
})
